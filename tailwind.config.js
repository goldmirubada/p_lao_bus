/** @type {import('tailwindcss').Config} */
module.exports = {
    darkMode: 'class', // Disable 'media' (system) strategy
    content: [
        "./src/**/*.{js,ts,jsx,tsx,mdx}",
    ],
    theme: {
        extend: {},
    },
    plugins: [],
}
